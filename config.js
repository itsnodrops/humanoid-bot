export default {
    // Loop Settings
    // Set ENABLE_LOOP to true to run continuously, false for single run
    ENABLE_LOOP: false,

    // LOOP_TIME in HH:MM format - when to run next cycle (based on local time)
    // Only used when ENABLE_LOOP is true
    LOOP_TIME: '07:00'
};